# Variant

Create a variation of an existing prototype.

## Your Task

{prompt}

## Directories

- **Source prototype** (READ-only): `{source_dir}`
- **Your output directory** (READ + WRITE): `{output_dir}`
- **All prototypes** (READ-only): `{prototypes_dir}`

## Instructions

1. Read the source prototype to understand its structure and behavior
2. Create a modified version in your output directory based on the task description
3. If no specific changes are requested, create an alternative approach to the same problem
4. Include a `package.json` with a `dev` script
5. Make it functional and visually clean
6. Use absolute paths for all file operations
