# Join

Combine two existing prototypes into a unified application.

## Your Task

{prompt}

## Directories

- **First child prototype** (READ-only): `{child1_dir}`
- **Second child prototype** (READ-only): `{child2_dir}`
- **Your output directory** (READ + WRITE): `{output_dir}`
- **All prototypes** (READ-only): `{prototypes_dir}`

## Instructions

1. Read both child prototypes to understand their features and structure
2. Combine their functionality into a single unified application in your output directory
3. Resolve any conflicts or overlaps between the two
4. Include a `package.json` with a `dev` script
5. Make it functional, cohesive, and visually clean
6. Use absolute paths for all file operations
