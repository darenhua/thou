# Decompose

Break a parent prototype into a focused sub-component.

## Your Task

{prompt}

## Directories

- **Parent prototype** (READ-only): `{parent_dir}`
- **Your output directory** (READ + WRITE): `{output_dir}`
- **All prototypes** (READ-only): `{prototypes_dir}`

## Instructions

1. Read the parent prototype to understand its structure and purpose
2. Extract or rebuild the specific sub-component described in the task
3. Create a standalone, working application in your output directory
4. Include a `package.json` with a `dev` script
5. Make it functional and self-contained
6. Use absolute paths for all file operations
